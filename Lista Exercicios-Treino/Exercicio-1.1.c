#include <stdio.h>
#include <stdlib.h>
#include "Exercicio-1.h"

int main(){
	int i, j;
	char s[100], aux[100]
	char *p
	
	gets(s);
	int tam = strlen(s)
	for(i = 0; i < tam; i++){
		if(isspace(s[i]){
			for(j = 0; j < i; j++) {
				aux[] = "";
				aux += s[j];
			}
			push(aux);
		}
	}

				
